<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <title>Pulse - APM</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <script type="module">
    // Ensure Stimulus controllers are initialized
    import("controllers/application").then(m => {
      console.log("[Layout] Stimulus initialized with", m.application.controllers.length, "controllers");
    }).catch(e => console.error("[Layout] Failed to initialize:", e));
  </script>
</head>
<body class="h-full antialiased" style="font-family: 'Inter', system-ui, sans-serif; background: #FAF9F7;">
  <div class="min-h-full">
    <header class="border-b" style="border-color: #E8E5E0; background: #FFFFFE;">
      <div class="mx-auto max-w-5xl px-6">
        <div class="flex h-16 items-center justify-between">
          <%= link_to dashboard_projects_path, class: "flex items-center gap-2.5" do %>
            <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
              <path d="M10 4C8.5 2 6 2 4.5 4C3 6 3 9 5 11L10 17L15 11C17 9 17 6 15.5 4C14 2 11.5 2 10 4Z" fill="#22C55E"/>
            </svg>
            <span class="text-[15px] font-semibold tracking-tight" style="color: #1A1A1A;">Pulse</span>
          <% end %>

          <% if @project&.persisted? %>
            <div class="flex items-center gap-6">
              <div class="flex items-center gap-3">
                <span style="color: #C4C0B8;">/</span>
                <%= link_to @project.name || 'Project', dashboard_overview_path, class: "text-[14px] hover:underline", style: "color: #6B6760;" %>
              </div>
              <nav class="flex items-center gap-4 text-[13px]">
                <%= link_to "Overview", dashboard_overview_path, class: "font-medium transition-colors", style: "color: #{controller_name == 'overview' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Requests", dashboard_requests_path, class: "font-medium transition-colors", style: "color: #{controller_name == 'requests' || controller_name == 'traces' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Jobs", dashboard_jobs_path, class: "font-medium transition-colors", style: "color: #{controller_name == 'jobs' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Endpoints", dashboard_endpoints_path, class: "font-medium transition-colors", style: "color: #{controller_name == 'endpoints' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Queries", dashboard_queries_path, class: "font-medium transition-colors", style: "color: #{controller_name == 'queries' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Metrics", dashboard_metrics_path, class: "font-medium transition-colors", style: "color: #{controller_name == 'metrics' ? '#1A1A1A' : '#8B8780'};" %>
                <%= link_to "Alerts", dashboard_alerts_path, class: "font-medium transition-colors", style: "color: #{%w[alerts alert_rules notification_channels].include?(controller_name) ? '#1A1A1A' : '#8B8780'};" %>
              </nav>
              <%= link_to dashboard_projects_path, class: "text-[13px] font-medium px-3 py-1.5 rounded-lg transition-colors hover:opacity-80", style: "background: #F0EFED; color: #6B6760;" do %>
                Projects
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </header>

    <main class="mx-auto max-w-5xl px-6 py-10">
      <%= yield %>
    </main>
  </div>
</body>
</html>
